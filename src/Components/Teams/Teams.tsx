import React from "react";

const Teams = () => {
  return (
    <section
      style={{
        height: "100vh",
        backgroundColor: "red",
        scrollMarginBlock: 0,
        scrollSnapAlign: "start none",
      }}
      id="teams"
    >
      Teams
    </section>
  );
};

export default Teams;
